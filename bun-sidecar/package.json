{
	"name": "swift-kit-bun-sidecar",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"build": "bun build ./src/index.ts --compile --outfile swift-kit-bun-sidecar --target=bun-windows-x64 && bun run build.ts",
		"build-min": "bun build ./src/index.ts --compile --outfile swift-kit-bun-sidecar --target=bun-windows-x64 --minify && bun run build.ts"
	},
	"dependencies": {
		"mime-types": "^2.1.35",
		"sanitize-filename": "^1.6.3"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/mime-types": "^2.1.4"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
